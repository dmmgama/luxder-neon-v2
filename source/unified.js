let wheel={enabled:[true,true,true,true],result:null,spinning:false,started:0,duration:0,angle:0},wheelTimer=null;
const wheelModes=[['Jogo da bicicleta','Bicycle game','setup'],['Amor de laboratório','Laboratory love','quiz-start'],['Tenta convencer-me','Try to convince me','convince-start'],['Grill me para dating','Grill me for dating','grill-start']];
function clearWheel(){clearTimeout(wheelTimer);wheelTimer=null;wheel.spinning=false;}
function themeBar(){return `<div class="themebar"><span>${t('Visual','Look')}</span><div class="lang" aria-label="${t('Escolher visual','Choose appearance')}"><button data-action="theme" data-value="neon" class="${theme==='neon'?'selected':''}" aria-pressed="${theme==='neon'}">${t('Néon','Neon')}</button><button data-action="theme" data-value="receipt" class="${theme==='receipt'?'selected':''}" aria-pressed="${theme==='receipt'}">${t('Talão','Receipt')}</button></div><button class="exit" data-action="modes">${t('4 modos ↗','4 modes ↗')}</button></div>`}
function wheelView(){const elapsed=wheel.spinning?Math.min(Date.now()-wheel.started,wheel.duration):0;return `${back()}${eyebrow('O acaso tem péssimas ideias','Chance has terrible ideas')}${heading('Roda da sorte.<br>Zero destino escrito.','Wheel of fortune.<br>No destiny attached.')}<p class="sub">${t('Escolhe os jogos que entram na roda. O acaso sugere; tu decides se jogas.','Choose which games enter the wheel. Chance suggests; you decide whether to play.')}</p><div class="wheel-stage"><span class="wheel-pointer" aria-hidden="true">▼</span><div class="fortune-wheel ${wheel.spinning?'spinning':''}" style="--angle:${wheel.angle}deg;--duration:${wheel.duration}ms;--delay:-${elapsed}ms;transform:rotate(${wheel.spinning?0:wheel.angle}deg)" aria-hidden="true"><span class="sector s0">01<br>🚲</span><span class="sector s1">02<br>♡</span><span class="sector s2">03<br>↗</span><span class="sector s3">04<br>◎</span><span class="wheel-hub">?</span></div></div><div class="wheel-choices">${wheelModes.map((m,i)=>`<button class="chip ${wheel.enabled[i]?'active':''}" data-action="wheel-toggle" data-value="${i}" aria-pressed="${wheel.enabled[i]}" ${wheel.spinning?'disabled':''}>${wheel.enabled[i]?'✓':'○'} ${i+1}. ${t(m[0],m[1])}</button>`).join('')}</div><div role="status" aria-live="polite">${wheel.spinning?`<p class="sub" style="text-align:center">${t('O universo está a improvisar…','The universe is improvising…')}</p>`:wheel.result!==null?`<div class="wheel-result"><div class="caption">${t('Saiu na roda','The wheel chose')}</div><h2>${t(...wheelModes[wheel.result])}</h2><p class="tagline">${t('Só uma sugestão. Podes rodar de novo ou escolher outro.','Just a suggestion. Spin again or choose something else.')}</p></div>`:''}</div><div class="stack">${wheel.result!==null&&!wheel.spinning?btn('Bora jogar ↗','Let’s play ↗','wheel-play'):''}<button class="btn ${wheel.result!==null?'secondary':''}" data-action="wheel-spin" ${wheel.spinning||!wheel.enabled.some(Boolean)?'disabled':''}>${wheel.spinning?t('A rodar…','Spinning…'):wheel.result!==null?t('Rodar outra vez ↻','Spin again ↻'):t('Rodar a sorte ↻','Spin the wheel ↻')}</button>${btn('Prefiro escolher','I’d rather choose','modes','','secondary small')}</div>${!wheel.enabled.some(Boolean)?`<p class="privacy">${t('Ativa pelo menos um jogo para rodar.','Enable at least one game to spin.')}</p>`:''}`}
function spinWheel(){if(wheel.spinning)return;const choices=wheel.enabled.flatMap((on,i)=>on?[i]:[]);if(!choices.length)return;clearWheel();wheel.result=null;const result=pick(choices);wheel.spinning=true;wheel.started=Date.now();wheel.duration=matchMedia('(prefers-reduced-motion:reduce)').matches?100:2800;wheel.angle=1440+(360-result*90)%360;render();wheelTimer=setTimeout(()=>{wheelTimer=null;if(screen!=='wheel')return;wheel.spinning=false;wheel.result=result;render();buzz();celebrate()},wheel.duration)}
function unifiedAction(action,value){if(action==='theme'){theme=value;document.body.classList.toggle('receipt',theme==='receipt');document.body.classList.toggle('neon',theme==='neon');render();return true}if(action==='wheel-open'){setMode('home');wheel.result=null;go('wheel');return true}if(action==='wheel-toggle'){if(!wheel.spinning){wheel.enabled[+value]=!wheel.enabled[+value];wheel.result=null;render()}return true}if(action==='wheel-spin'){spinWheel();return true}if(action==='wheel-play'){if(wheel.result!==null&&!wheel.spinning){const next=wheelModes[wheel.result][2];if(next==='setup'){setMode('game');go('setup')}else extraAction(next,'')}return true}return false}
